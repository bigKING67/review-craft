# Real-repository verifier boundary

This directory is intentionally oracle-free. It exists as the evidence-loop adapter's
separate verifier-access boundary but contains no upstream fix revision, expected probe
disposition, severity, decision, or adjudication answer. Campaign output is validated by the
host runner, and independent adjudication happens only after treatment execution.
